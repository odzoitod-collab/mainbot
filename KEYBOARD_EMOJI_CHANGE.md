# Изменение смайлика при установке клавиатуры

## Что изменилось
При нажатии кнопок статической клавиатуры (🏠 Главное меню, 👤 Профиль, 🛠 Сервисы) бот теперь отправляет смайлик 📱 вместо точки.

## Файл изменения
`mainbot/utils/messages.py` - функция `answer_with_brand()`

## Изменение
```python
# Было:
await message.answer(".", reply_markup=static_keyboard, parse_mode="HTML")

# Стало:
await message.answer("📱", reply_markup=static_keyboard, parse_mode="HTML")
```

## Где это работает
- При нажатии кнопки "🏠 Главное меню"
- При нажатии кнопки "👤 Профиль" 
- При нажатии кнопки "🛠 Сервисы"
- При любом другом использовании статической клавиатуры

## Результат
Пользователи будут видеть смайлик 📱 вместо точки при переходах по меню бота.

## Техническая информация
Сообщение со смайликом отправляется для установки ReplyKeyboardMarkup (статической клавиатуры) в приватных чатах и затем пытается удалиться через 0.1 секунды.