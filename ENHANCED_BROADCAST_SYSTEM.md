# Улучшенная система рассылки

## Обзор
Система рассылки теперь поддерживает специальные кнопки с командами, включая перезапуск бота и быстрый доступ к функциям.

## Новые возможности

### 🔗 Обычные ссылки
```
Текст кнопки | https://example.com
```

### ⚡ Специальные команды
```
Перезапуск | restart      - Перезапускает бота
Сервисы | services        - Открывает список сервисов
Профиль | profile         - Показывает профиль пользователя
Помощь | help             - Показывает список команд
Топ | top                 - Показывает топ воркеров
Меню | menu               - Открывает главное меню
```

## Как использовать

### 1. Создание рассылки
1. Админ панель → Рассылка
2. Выберите тип (текст/фото)
3. Введите заголовок
4. Введите текст
5. **Добавьте кнопку** (новый шаг)

### 2. Форматы кнопок

#### Обычная ссылка:
```
Открыть сайт | https://example.com
```

#### Перезапуск бота:
```
Обновить бот | restart
```

#### Быстрые команды:
```
Мои сервисы | services
Мой профиль | profile
Нужна помощь | help
Посмотреть топ | top
```

### 3. Пропуск кнопки
Введите `-` чтобы отправить рассылку без кнопки.

## Система перезапуска

### Как работает
1. Пользователь нажимает кнопку "Перезапуск"
2. Бот отправляет уведомления админам
3. Создается флаг перезапуска
4. Бот корректно завершает работу
5. Supervisor/Docker перезапускает процесс
6. При старте бот уведомляет о завершении перезапуска

### Настройка перезапуска

#### Для Docker:
```yaml
# docker-compose.yml
restart: unless-stopped
```

#### Для Supervisor:
```ini
[program:bot]
command=python main.py
directory=/path/to/bot
autorestart=true
```

#### Для Systemd:
```ini
[Unit]
Description=Telegram Bot
After=network.target

[Service]
Type=simple
User=bot
WorkingDirectory=/path/to/bot
ExecStart=python main.py
Restart=always

[Install]
WantedBy=multi-user.target
```

### Переменные окружения
```bash
# Для webhook перезапуска
RESTART_WEBHOOK_URL=https://your-server.com/restart

# Для команды перезапуска
RESTART_COMMAND="systemctl restart bot"

# Для supervisor
SUPERVISOR_ENABLED=true

# Для systemd
SYSTEMD_ENABLED=true
```

## Обработчики специальных кнопок

### restart_bot
- Создает флаг перезапуска
- Уведомляет админов
- Корректно завершает процесс

### broadcast_services
- Показывает список сервисов
- Использует существующую функцию

### broadcast_profile
- Показывает профиль пользователя
- Работает как команда /me

### broadcast_help
- Показывает список команд
- Работает как команда /help

### broadcast_top
- Показывает топ воркеров
- Работает как команда /top

## Примеры использования

### Уведомление об обновлении:
```
Заголовок: Обновление бота
Текст: Вышла новая версия бота с улучшениями!
Кнопка: Перезапустить | restart
```

### Напоминание о сервисах:
```
Заголовок: Не забудьте про работу
Текст: Проверьте доступные сервисы для заработка
Кнопка: Мои сервисы | services
```

### Мотивационное сообщение:
```
Заголовок: Как дела с профитами?
Текст: Посмотрите свою позицию в топе!
Кнопка: Посмотреть топ | top
```

## Безопасность

### Ограничения доступа
- Только админы могут создавать рассылки
- Перезапуск доступен только через админ-панель
- Все действия логируются

### Защита от злоупотреблений
- Проверка прав доступа
- Логирование всех действий
- Ограничение частоты перезапусков

## Мониторинг

### Логи
Все действия записываются в логи:
```
INFO: Restart flag created by user 123456: broadcast_button
INFO: Restart webhook called successfully
INFO: Found restart flag: restart_requested_by_123456_broadcast_button
```

### Уведомления
Админы получают уведомления:
- При запросе перезапуска
- При завершении перезапуска
- При ошибках

## Устранение неполадок

### Бот не перезапускается
1. Проверьте настройки supervisor/docker
2. Убедитесь что процесс автоматически перезапускается
3. Проверьте права на создание файлов

### Кнопки не работают
1. Проверьте формат: `Текст | команда`
2. Убедитесь что команда из списка поддерживаемых
3. Проверьте логи на ошибки

### Рассылка не доходит
1. Проверьте права бота
2. Убедитесь что пользователи не заблокировали бота
3. Проверьте лимиты Telegram API

## Расширение функциональности

### Добавление новых команд
1. Добавьте команду в список в `receive_button()`
2. Создайте обработчик `@router.callback_query(F.data == "your_command")`
3. Реализуйте логику обработки

### Пример новой команды:
```python
elif btn_action.lower() in ["stats", "статистика"]:
    btn_url = "broadcast_stats"
    btn_type = "callback"

@router.callback_query(F.data == "broadcast_stats")
async def handle_broadcast_stats(callback: CallbackQuery) -> None:
    # Ваша логика
    pass
```