# Изменения в системе рассылки

## 🎯 Что добавлено

### 1. Специальные кнопки в рассылке
- **Перезапуск бота** (`restart`) - корректный перезапуск с уведомлениями
- **Сервисы** (`services`) - быстрый доступ к списку сервисов
- **Профиль** (`profile`) - показ профиля пользователя
- **Помощь** (`help`) - список команд бота
- **Топ** (`top`) - топ воркеров
- **Меню** (`menu`) - главное меню бота

### 2. Система перезапуска
- Корректное завершение процесса
- Уведомления админам
- Флаги перезапуска
- Поддержка Docker/Supervisor/Systemd

### 3. Улучшенный интерфейс
- Подробные инструкции по созданию кнопок
- Предпросмотр с описанием действий
- Валидация команд

## 📁 Измененные файлы

### handlers/admin_broadcast.py
- ✅ Добавлена поддержка callback кнопок
- ✅ Обработчики специальных команд
- ✅ Улучшенная валидация
- ✅ Подробные инструкции

### utils/restart.py (новый)
- ✅ RestartManager класс
- ✅ Создание/проверка флагов перезапуска
- ✅ Уведомления админам
- ✅ Поддержка разных методов перезапуска

### config.py
- ✅ Настройки системы перезапуска
- ✅ Параметры рассылки

### main.py
- ✅ Инициализация RestartManager
- ✅ Проверка флагов при старте

## 🔧 Новые функции

### Формат кнопок
```python
# Обычная ссылка
"Текст | https://example.com"

# Специальные команды
"Перезапуск | restart"
"Сервисы | services"
"Профиль | profile"
"Помощь | help"
"Топ | top"
"Меню | menu"
```

### Обработчики команд
```python
@router.callback_query(F.data == "restart_bot")
@router.callback_query(F.data == "broadcast_services")
@router.callback_query(F.data == "broadcast_profile")
@router.callback_query(F.data == "broadcast_help")
@router.callback_query(F.data == "broadcast_top")
```

### RestartManager
```python
restart_manager = RestartManager(bot)
await restart_manager.request_restart(user_id, reason)
await restart_manager.check_and_handle_restart_flag()
```

## 🚀 Как использовать

### 1. Создание рассылки с кнопкой перезапуска
```
Админ панель → Рассылка → Текст
Заголовок: Обновление бота
Текст: Вышла новая версия!
Кнопка: Перезапустить | restart
```

### 2. Рассылка с быстрым доступом
```
Заголовок: Проверьте сервисы
Текст: Новые возможности для заработка
Кнопка: Мои сервисы | services
```

### 3. Мотивационная рассылка
```
Заголовок: Как дела с профитами?
Текст: Посмотрите свою позицию!
Кнопка: Посмотреть топ | top
```

## ⚙️ Настройка перезапуска

### Docker Compose
```yaml
services:
  bot:
    restart: unless-stopped
    # Автоматический перезапуск при exit code 1
```

### Supervisor
```ini
[program:bot]
command=python main.py
autorestart=true
exitcodes=0,1
```

### Systemd
```ini
[Service]
Restart=always
RestartSec=5
```

### Переменные окружения
```bash
# Webhook для перезапуска
RESTART_WEBHOOK_URL=https://your-server.com/restart

# Команда перезапуска
RESTART_COMMAND="systemctl restart bot"

# Флаги окружения
SUPERVISOR_ENABLED=true
SYSTEMD_ENABLED=true
```

## 🔒 Безопасность

### Ограничения доступа
- Только админы могут создавать рассылки
- Только админы могут инициировать перезапуск
- Все действия логируются в базу данных

### Защита от злоупотреблений
- Проверка прав на каждом этапе
- Логирование всех действий перезапуска
- Уведомления всем админам

### Мониторинг
- Логи в консоль и файлы
- Уведомления в Telegram
- Отслеживание флагов перезапуска

## 📊 Статистика рассылки

### Улучшенная отчетность
- Количество успешных отправок
- Количество заблокированных ботов
- Количество ошибок
- Процент доставляемости

### Прогресс в реальном времени
- Обновление каждые 20 сообщений
- Показ текущего прогресса
- Финальная статистика

## 🐛 Устранение неполадок

### Бот не перезапускается
1. Проверьте настройки контейнера/сервиса
2. Убедитесь в автоматическом перезапуске
3. Проверьте права на создание файлов

### Кнопки не работают
1. Проверьте формат: `Текст | команда`
2. Используйте только поддерживаемые команды
3. Проверьте логи на ошибки

### Рассылка не доходит
1. Проверьте лимиты Telegram API
2. Убедитесь что пользователи не заблокировали бота
3. Проверьте права бота в чатах

## 🔄 Процесс перезапуска

### Что происходит
1. **Запрос** - админ нажимает кнопку
2. **Уведомление** - все админы получают сообщение
3. **Флаг** - создается файл restart_flag.txt
4. **Завершение** - бот корректно завершается (exit 1)
5. **Перезапуск** - Docker/Supervisor запускает новый процесс
6. **Проверка** - новый процесс находит флаг
7. **Уведомление** - админы получают сообщение о завершении
8. **Очистка** - флаг удаляется

### Время перезапуска
- **Docker**: 5-15 секунд
- **Supervisor**: 10-20 секунд  
- **Systemd**: 15-30 секунд

## 📈 Будущие улучшения

### Планируемые функции
- Отложенная рассылка
- Рассылка по группам пользователей
- Шаблоны сообщений
- A/B тестирование рассылок
- Статистика кликов по кнопкам

### Дополнительные команды
- Статистика (`stats`)
- Настройки (`settings`)
- Экспорт данных (`export`)
- Бэкап (`backup`)

Система готова к использованию! 🎉