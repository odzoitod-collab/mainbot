# Использование тегов в уведомлениях о профитах

## Изменения

Теперь во всех уведомлениях о профитах вместо никнейма (@username) отображается тег пользователя.

## Где изменено:

### 1. Канал профитов
**Было:** `▫️ПРОФИТ от @username`  
**Стало:** `▫️ПРОФИТ от #user_tag`

### 2. Уведомление наставнику
**Было:** `┠• 👤 Воркер: @username`  
**Стало:** `┠• 👤 Воркер: #user_tag`

### 3. Уведомление рефереру
**Было:** `┠• 👤 Реферал: @username`  
**Стало:** `┠• 👤 Реферал: #user_tag`

### 4. Создание профита админом
При выборе воркера теперь показывается:  
`╭• ✅ Воркер: Имя (@username) #user_tag`

## Технические изменения:

### mainbot/handlers/admin_profit.py

1. **receive_worker()** - добавлено сохранение тега:
```python
user_tag = user.get('user_tag', '#irl_???')
await state.update_data(
    worker_id=user["id"], 
    worker_username=user["username"], 
    worker_name=user["full_name"],
    worker_tag=user_tag
)
```

2. **send_profit_to_channel()** - добавлен параметр `worker_tag`:
```python
async def send_profit_to_channel(
    bot: Bot,
    profit_id: int,
    worker_name: str,
    worker_username: str,
    worker_tag: str,  # Новый параметр
    service_name: str,
    amount: float,
    net_profit: float,
    percent: int
) -> bool:
```

3. **Вызов send_profit_to_channel()** - передается тег:
```python
await send_profit_to_channel(
    callback.bot,
    profit_id=profit_id,
    worker_name=data['worker_name'],
    worker_username=data['worker_username'],
    worker_tag=data.get('worker_tag', '#irl_???'),  # Передаем тег
    service_name=data['service_name'],
    amount=amount,
    ...
)
```

4. **Уведомления наставнику и рефереру** - используют тег:
```python
f"┠• 👤 Воркер: {data.get('worker_tag', '#irl_???')}\n"
f"┠• 👤 Реферал: {data.get('worker_tag', '#irl_???')}\n"
```

## Результат

✅ Все уведомления о профитах теперь показывают тег вместо никнейма  
✅ Единообразие отображения пользователей по всему боту  
✅ Конфиденциальность - никнеймы не раскрываются в публичном канале  
✅ Fallback на `#irl_???` если тег не установлен  

## Примеры:

**Канал профитов:**
```
▫️ПРОФИТ от #irl_boss
  ╭• 🛠 Сервис: Сервис 1
  ╰• 🏳️ Страна: Россия🇷🇺
➘ 💰 Сумма: 10,000.00 ₽
➘ 💸 Доля воркера (50%): 5,000.00 ₽
```

**Наставнику:**
```
╭• 🦢 ПРОФИТ ОТ УЧЕНИКА
┠• 👤 Воркер: #irl_boss
┖• 💸 Ваша доля: 500.00 RUB
```

**Рефереру:**
```
╭• 🔗 РЕФЕРАЛЬНЫЙ ДОХОД
┠• 👤 Реферал: #irl_boss
┖• 💸 Ваша доля: 250.00 RUB
```
