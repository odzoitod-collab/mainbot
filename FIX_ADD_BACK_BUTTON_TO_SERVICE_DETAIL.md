# Добавление кнопки "Назад" к детальному просмотру сервиса

## Изменение

Добавлена кнопка "🔙 К списку сервисов" в детальный просмотр сервиса.

## Было:

```
[Детальный просмотр сервиса]
📖 Открыть мануал | 🤖 Перейти к боту
[Нет кнопки назад]
```

## Стало:

```
[Детальный просмотр сервиса]
📖 Открыть мануал | 🤖 Перейти к боту
🔙 К списку сервисов
```

## Технические детали

### Изменена функция `get_service_detail_keyboard()`:

```python
def get_service_detail_keyboard(service_id: int, manual_link: str = None, bot_link: str = None):
    buttons = []
    
    # Ссылки на мануал и бот
    if manual_link:
        buttons.append([InlineKeyboardButton(text="📖 Открыть мануал", url=manual_link)])
    if bot_link:
        buttons.append([InlineKeyboardButton(text="🤖 Перейти к боту", url=bot_link)])
    
    # Кнопка "Назад к сервисам"
    buttons.append([InlineKeyboardButton(text="🔙 К списку сервисов", callback_data="services")])
    
    return InlineKeyboardMarkup(inline_keyboard=buttons)
```

### Callback обработчик:

Кнопка использует существующий обработчик `show_services()` в `user_menu.py`:

```python
@router.callback_query(F.data == "services")
async def show_services(callback: CallbackQuery) -> None:
    await callback.answer()
    
    services = await get_services()
    
    text = (
        f"{header('Сервисы', '🛠')}\n\n"
        f"🎯 Рабочие инструменты для заработка\n\n"
        f"Выбери нужный сервис из списка ниже.\n"
        f"Каждый сервис содержит мануалы и боты для работы."
    )
    
    await edit_with_brand(callback, text, reply_markup=get_services_keyboard(services), image_path=BRAND_IMAGE_SERVICES)
```

## Преимущества

✅ **Улучшенная навигация** - пользователь может легко вернуться к списку сервисов  
✅ **Единообразие** - все детальные просмотры теперь имеют кнопку "Назад"  
✅ **UX** - не нужно искать как вернуться назад  

## Измененные файлы

- `mainbot/keyboards/user_kb.py`

## Где работает

- Детальный просмотр любого сервиса
- Доступно из главного меню → Сервисы → [Выбор сервиса]
- Также доступно из команды `/сервисы` в чате

## Результат

Теперь навигация по сервисам стала удобнее! 🎯
