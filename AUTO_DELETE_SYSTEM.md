# СИСТЕМА АВТОУДАЛЕНИЯ СООБЩЕНИЙ В ЧАТЕ

## Обзор
Система автоматического удаления сообщений бота в групповых чатах через 10 секунд. Это помогает поддерживать чистоту чата и избежать спама от команд.

## Основные функции

### 🎯 Автоудаление в групповых чатах
- **Работает только в группах**: В личных сообщениях работает как обычно
- **Задержка**: 10 секунд по умолчанию (настраивается)
- **Удаление команды**: Опционально удаляет и команду пользователя
- **Безопасность**: Обработка ошибок при удалении

### 📝 Типы сообщений
- **Текстовые**: Обычные текстовые ответы
- **С изображениями**: Ответы с фото и подписью
- **С клавиатурами**: Сообщения с inline кнопками

## Реализация

### Утилиты (utils/auto_delete.py)

#### reply_with_auto_delete()
```python
await reply_with_auto_delete(
    message,
    "Текст ответа",
    delay=10,                    # Задержка перед удалением
    delete_original=True,        # Удалить команду пользователя
    parse_mode='HTML'            # Дополнительные параметры
)
```

#### reply_photo_with_auto_delete()
```python
await reply_photo_with_auto_delete(
    message,
    photo=FSInputFile("image.jpg"),
    caption="Подпись",
    delay=10,
    delete_original=True,
    reply_markup=keyboard
)
```

### Логика работы

1. **Проверка типа чата**: Определяет групповой или личный чат
2. **Отправка сообщения**: Отправляет ответ пользователю
3. **Планирование удаления**: Создает асинхронную задачу удаления
4. **Ожидание**: Ждет указанное время (10 сек)
5. **Удаление**: Удаляет сообщение бота и опционально команду

### Обработка ошибок

```python
try:
    await bot.delete_message(chat_id, message_id)
except TelegramBadRequest as e:
    if "message to delete not found" not in str(e).lower():
        logger.warning(f"Failed to delete message: {e}")
```

## Применение к командам

### Обновленные команды
Все команды в `handlers/chat_commands.py` обновлены:

- `/help` - Список команд
- `/me` - Профиль пользователя
- `/card` - Реквизиты
- `/top`, `/topm`, `/topw`, `/topd` - Топы
- `/kasa` - Касса команды
- `/kurator` - Наставники
- `/сервисы` - Сервисы
- `/ресурсы` - Материалы
- `/сообщество` - Чаты
- `/идеи` - Идеи
- `/инфо` - Информация
- `/правила` - Правила
- `/поддержка` - Поддержка
- `/реф` - Реферальная программа
- `/changetag` - Смена тега
- `/быстро` - Быстрые команды
- `/stats` - Статистика (админ)

### Пример обновления

**До:**
```python
@router.message(Command("help"))
async def cmd_help(message: Message) -> None:
    await message.reply("Текст помощи")
```

**После:**
```python
@router.message(Command("help"))
async def cmd_help(message: Message) -> None:
    await reply_with_auto_delete(
        message,
        "Текст помощи",
        delay=10,
        delete_original=True
    )
```

## Настройка

### Изменение задержки
```python
# Удаление через 5 секунд
await reply_with_auto_delete(message, "Текст", delay=5)

# Удаление через 30 секунд
await reply_with_auto_delete(message, "Текст", delay=30)
```

### Сохранение команды пользователя
```python
# Не удалять команду пользователя
await reply_with_auto_delete(
    message,
    "Текст",
    delay=10,
    delete_original=False
)
```

### Отключение автоудаления
Для отключения автоудаления в конкретной команде используйте обычные методы:
```python
# Сообщение не будет удалено
await message.reply("Текст")
```

## Преимущества

### 🧹 Чистота чата
- Автоматическое удаление команд и ответов
- Чат остается чистым и читаемым
- Меньше спама от команд

### 🔒 Приватность
- Команды пользователей удаляются
- Ответы бота не остаются в истории
- Конфиденциальная информация не сохраняется

### ⚡ Производительность
- Асинхронное удаление
- Не блокирует основной поток
- Обработка ошибок

### 🎯 Гибкость
- Настраиваемая задержка
- Опциональное удаление команд
- Работает только в группах

## Технические детали

### Определение типа чата
```python
def is_group_chat(message: Message) -> bool:
    """Check if message is from a group chat."""
    return message.chat.type in ['group', 'supergroup']
```

### Асинхронное удаление
```python
async def delete_messages_after_delay(
    bot: Bot,
    chat_id: int,
    message_ids: list,
    delay: int = 10
):
    await asyncio.sleep(delay)
    for message_id in message_ids:
        try:
            await bot.delete_message(chat_id, message_id)
        except TelegramBadRequest:
            pass  # Сообщение уже удалено или недоступно
```

### Планирование задачи
```python
asyncio.create_task(
    delete_messages_after_delay(
        message.bot,
        message.chat.id,
        [sent_message.message_id, message.message_id],
        delay
    )
)
```

## Логирование

### Успешное удаление
```
DEBUG: Deleted message 12345 in chat -100123456789
```

### Ошибки
```
WARNING: Failed to delete message 12345: Message to delete not found
```

## Ограничения

### Права бота
Бот должен иметь права на удаление сообщений в группе:
- `can_delete_messages` - для удаления своих сообщений
- `can_delete_messages` (admin) - для удаления сообщений пользователей

### Временные ограничения
- Сообщения старше 48 часов не могут быть удалены
- Закрепленные сообщения требуют дополнительных прав

### Типы чатов
- Работает в: `group`, `supergroup`
- Не работает в: `private`, `channel`

## Тестирование

### Проверка в группе
1. Добавьте бота в группу
2. Дайте права на удаление сообщений
3. Отправьте команду `/help`
4. Через 10 секунд сообщения должны удалиться

### Проверка в личке
1. Откройте личный чат с ботом
2. Отправьте команду `/help`
3. Сообщение должно остаться (не удаляться)

## Будущие улучшения

### Планируемые функции
- **Настройка через конфиг**: Глобальная настройка задержки
- **Белый список команд**: Команды которые не удаляются
- **Статистика**: Количество удаленных сообщений
- **Уведомления**: Предупреждение перед удалением

### Технические улучшения
- **Кэширование прав**: Проверка прав бота перед удалением
- **Batch deletion**: Групповое удаление сообщений
- **Retry logic**: Повторные попытки при ошибках
- **Метрики**: Мониторинг работы системы

## Примеры использования

### Команда с фото
```python
@router.message(Command("profile"))
async def cmd_profile(message: Message):
    photo = FSInputFile("profile.jpg")
    await reply_photo_with_auto_delete(
        message,
        photo=photo,
        caption="Ваш профиль",
        delay=10,
        delete_original=True
    )
```

### Команда с клавиатурой
```python
@router.message(Command("menu"))
async def cmd_menu(message: Message):
    keyboard = InlineKeyboardMarkup(...)
    await reply_with_auto_delete(
        message,
        "Выберите действие:",
        reply_markup=keyboard,
        delay=10,
        delete_original=True
    )
```

### Команда без удаления оригинала
```python
@router.message(Command("info"))
async def cmd_info(message: Message):
    await reply_with_auto_delete(
        message,
        "Информация о боте",
        delay=15,
        delete_original=False  # Команда останется
    )
```

## Заключение

Система автоудаления сообщений помогает поддерживать чистоту групповых чатов, автоматически удаляя команды и ответы бота через 10 секунд. Система работает только в группах, не затрагивая личные сообщения, и полностью настраивается под нужды проекта.